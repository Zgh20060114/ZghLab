一个类里的一个成员变量是一个裸指针,导致还要手写拷贝构造函数析构函数等这5个函数.那怎么用智能指针不用写这些函数呢?
- 使用裸指针的5法则反面教材:
```cpp
class BadBuffer {
    char* m_data;
    size_t m_size;
public:
    // 1. 构造函数
    explicit BadBuffer(size_t size) : m_size(size), m_data(new char[size]) {}
    
    // 2. 析构函数（必须手写，防泄漏）
    ~BadBuffer() { delete[] m_data; } 
    
    // 3. 拷贝构造（必须手写，防浅拷贝 double free）
    BadBuffer(const BadBuffer& other) : m_size(other.m_size), m_data(new char[other.m_size]) {
        std::memcpy(m_data, other.m_data, m_size);
    }
    
    // 4. 拷贝赋值（必须手写）
    BadBuffer& operator=(const BadBuffer& other) {
        if (this != &other) { // 防止p1 = p1;
            delete[] m_data;
            m_size = other.m_size;
            m_data = new char[m_size];
            std::memcpy(m_data, other.m_data, m_size);
        }
        return *this;
    }
    
    // 5. 移动构造（必须手写，防性能暴跌）
    BadBuffer(BadBuffer&& other) noexcept : m_data(other.m_data), m_size(other.m_size) {
        other.m_data = nullptr;
        other.m_size = 0;
    }
    // 6. 移动赋值运算符（必须手写，防内存泄漏和 Double Free）
    BadBuffer& operator=(BadBuffer&& other) noexcept {
        if (this != &other) {             // 1. 检查自赋值
            delete[] m_data;              // 2. 释放当前对象原有的内存（防泄漏）
            m_data = other.m_data;        // 3. 接管对方的内存
            m_size = other.m_size;
            other.m_data = nullptr;       // 4. 掏空对方，防 Double Free
            other.m_size = 0;
        }
        return *this;                     // 5. 返回引用，支持链式赋值
    }
};
```
```
```
- 使用智能指针的0法则正面教材(独享资源,不用拷贝):
```cpp
#include <memory>

class GoodBuffer {
    std::unique_ptr<char[]> m_data; // 智能指针接管内存
    size_t m_size;
public:
    // 唯一需要手写的：构造函数
    explicit GoodBuffer(size_t size) 
        : m_size(size), m_data(std::make_unique<char[]>(size)) {}

    // 析构函数？不需要！unique_ptr 销毁时自动 delete[]
    // 拷贝构造？不需要！unique_ptr 独占资源，禁止拷贝，编译器自动 delete
    // 拷贝赋值？不需要！同上
    // 移动构造？不需要！unique_ptr 内部已实现移动语义，编译器自动调用
    // 移动赋值？不需要！同上
};
```
- 使用智能指针的0法则正面教材(独享资源,不用拷贝):
```cpp
#include <memory>

class CopyableBuffer {
    std::shared_ptr<char[]> m_data; // 共享所有权
    size_t m_size;
public:
    explicit CopyableBuffer(size_t size) 
        : m_size(size), m_data(std::make_shared<char[]>(size)) {}

    // 如果需要深拷贝，只需手写拷贝构造，利用 make_shared 重新分配
    CopyableBuffer(const CopyableBuffer& other) 
        : m_size(other.m_size), m_data(std::make_shared<char[]>(other.m_size)) {
        std::memcpy(m_data.get(), other.m_data.get(), m_size);
    }
    
    // 析构、移动、赋值依然不需要手写！
};
```
